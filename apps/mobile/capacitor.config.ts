import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.im.chat",
  appName: "im-chat-mobile",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
